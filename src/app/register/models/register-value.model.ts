export class RegisterValue {

    email!: {
        email: string,
        confirm: string
    };

    loginInfo!: {
        username: string,
        password: string,
        confirmPassword: string,
    };
}